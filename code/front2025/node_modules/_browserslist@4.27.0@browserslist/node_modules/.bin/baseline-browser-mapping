#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_baseline-browser-mapping@2.8.21@baseline-browser-mapping/dist/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_baseline-browser-mapping@2.8.21@baseline-browser-mapping/dist/cli.js" "$@"
  ret=$?
fi
exit $ret
